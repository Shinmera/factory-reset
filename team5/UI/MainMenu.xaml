<Page
    x:Class="team5.UI.MainMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:team5.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Style="{StaticResource MainMenu}">
    <Grid>
        <SplitView HorizontalAlignment="Stretch" IsPaneOpen="True" DisplayMode="Inline" OpenPaneLength="200"
                   PaneBackground="Transparent">
            <SplitView.Pane>
                <ListView x:Name="SidePanel" HorizontalAlignment="Stretch" SelectedIndex="0"
                          IsItemClickEnabled="True" SelectionMode="Single" SingleSelectionFollowsFocus="True"
                          ItemClick="ItemClick" SelectionChanged="SelectionChanged"
                          Style="{StaticResource SidePanel}" Background="#80FFFFFF">
                    <x:String>New Game</x:String>
                    <x:String>Load</x:String>
                    <x:String>Options</x:String>
                    <x:String>Quit</x:String>
                </ListView>
            </SplitView.Pane>
            <Grid x:Name="Content" Padding="10,10,10,10"/>
        </SplitView>
    </Grid>
</Page>