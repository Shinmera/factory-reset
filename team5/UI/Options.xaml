<Page
    x:Class="team5.UI.Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:team5.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">

    <Grid>
        <StackPanel HorizontalAlignment="Left" Width="200" Padding="10,0,10,0" VerticalAlignment="Center">
            <TextBlock Text="Options" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <Slider x:Name="masterVolume" Header="Master Volume" Maximum="100"
                    ValueChanged="MasterVolume_ValueChanged"/>
            <Button x:Name="back" Content="Back" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                Click="Back_Click" Height="32" Margin="-10,0,-10,0"
                Style="{StaticResource Back}"/>
        </StackPanel>

    </Grid>
</Page>
